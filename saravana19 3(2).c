#include<stdio.h>
#include<conio.h>
int main ()
{
int h,m,tm;
printf("enter the hours:");
scanf("%d",&h);
printf("enter the minutes:");
scanf("%d",&m);
for(i=0;i>=1;i++);
tm=(h*60)+m;
printf("total time in minutes is:",tm);
}
getch ();
return 0;
}
